"use client";

export const ControlBar = () => {
  return (
    <div className="control-bar">
      <button onClick={() => {}}>Start Conversion</button>
      <button onClick={() => {}}>Clear Queue</button>
    </div>
  );
};